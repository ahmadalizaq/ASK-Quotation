# ASK & Quotation (QQC) — دليل الإعداد والتشغيل

الموقع مقسّم لملفات منظمة حسب المسؤولية:

```
index.html          — الهيكل (markup) بس
style.css            — كل التصميم
config.js             — إعدادات الاتصال بـ Supabase (تعبيها أنت)
supabase-client.js    — ينشئ الاتصال بقاعدة البيانات
db.js                 — كل شي يتكلم مع قاعدة البيانات (حسابات، منشورات، لايكات، متابعة، رسائل...)
app.js                — كل شي يرسم الواجهة ويتعامل مع تفاعل المستخدم
manifest.json          — يخلي الموقع "قابل للتثبيت" كتطبيق على الشاشة الرئيسية (أندرويد/آيفون)
sw.js                  — Service Worker بسيط بدون أي كاش، بس عشان شرط التثبيت بأندرويد
icons/                 — أيقونات التطبيق (192px, 512px, maskable, apple-touch-icon)
schema.sql            — كود إنشاء قاعدة البيانات الأساسية
fix-comments-avatar-notifications.sql — ترقية التعليقات والصور الشخصية (شغّلتها قبل)
upgrade-v2.sql        — ترقية جديدة: رسائل خاصة، اقتباس مميز، نظام عملات، شوت أوت للجميع، هوية الاعتراف
```

**ملاحظة:** ملف `sw.js` رجع من جديد لكن بنسخة مختلفة تماماً — الحين ما يخزّن أي شي بالكاش إطلاقاً (كل طلب يروح للشبكة مباشرة)، غرضه الوحيد إنه يخلي المتصفح يعتبر الموقع قابل للتثبيت. ما فيه احتمال يرجع مشكلة الكاش العالق القديمة.

الملفات دي (`config.js`, `supabase-client.js`, `db.js`, `app.js`, `manifest.json`, `sw.js`, ومجلد `icons/` كامل) لازم تترفع كلها مع بعض بنفس المجلد مثل `index.html` — الموقع محتاجهم كلهم عشان يشتغل، وعشان ميزة "تثبيت التطبيق" تشتغل صح.

---

## تثبيت الموقع كتطبيق على الشاشة الرئيسية

الموقع الحين "PWA" — أي زائر يقدر يثبّته على شاشته الرئيسية ويفتح بدون شريط متصفح، زي أي تطبيق عادي:

- **أندرويد (كروم / سامسونج إنترنت):** يطلع بانر تلقائي بأسفل الشاشة "ثبّت QQC" — يضغط زر **تثبيت** وتظهر نافذة تثبيت أصلية من المتصفح نفسه، بضغطة وحدة.
- **آيفون (Safari):** ما فيه API يخلي المتصفح يعرض تثبيت تلقائي (قيد من آبل نفسها، مو تقصير بالموقع) — بيطلع نفس البانر، وبالضغط عليه تطلع تعليمات مصوّرة بثلاث خطوات: زر المشاركة ⬆️ ← "إضافة إلى الشاشة الرئيسية" ← "إضافة".

**رابط واحد يكفي للاثنين** — نفس رابط موقعك (بعد رفعه على Vercel) يشتغل لأندرويد وآيفون؛ الفرق بس بشكل التثبيت لأن آبل نفسها ما تسمح بتثبيت بضغطة وحدة على Safari لأي موقع بالعالم.

---

## تشغيل ترقية v2 (الميزات الجديدة)

1. Supabase → **SQL Editor → New query**.
2. افتح `upgrade-v2.sql`، انسخ محتواه كامل، الصقه، واضغط **Run**.
3. ارفع ملفات `index.html`, `style.css`, `db.js`, `app.js` المحدّثة (بدل القديمة) على GitHub — Vercel ينشرها تلقائياً خلال ثوانٍ.

هذا كل شي — الميزات التسع الجديدة تشتغل فوراً بعدها.

---

## الخطوة 1: إنشاء قاعدة البيانات (Supabase)


1. روح [supabase.com](https://supabase.com) → **Start your project** → سجّل دخول بحساب GitHub.
2. **New project** → اختر اسم، منظمة، وباسورد لقاعدة البيانات (احفظه) → أقرب Region → **Create new project**. انتظر دقيقتين.
3. **مهم**: من **Authentication → Settings**:
   - تجربة سريعة بدون تعقيد: **أوقف** "Confirm email".
   - موقع حقيقي جاهز للنشر: خلّه شغال (الافتراضي) — كل تسجيل جديد يحتاج تأكيد إيميل.
4. **SQL Editor → New query** → افتح `schema.sql` المرفق، انسخ محتواه كامل، الصقه، واضغط **Run**.
   هذا ينشئ 7 جداول (`profiles`, `posts`, `confessions`, `likes`, `confession_likes`, `follows`, `notifications`) بدون أي بيانات وهمية.
5. **Project Settings ⚙️ → API** → انسخ **Project URL** و **anon public** key.

---

## الخطوة 2: تعبئة الإعدادات

افتح `config.js` واستبدل:
```js
const SUPABASE_CONFIG = {
  url: "YOUR_SUPABASE_URL",       // ← حط الـ Project URL هنا
  anonKey: "YOUR_SUPABASE_ANON_KEY" // ← حط الـ anon key هنا
};
```

---

## الخطوة 3: رفعه أونلاين (GitHub + Vercel)

1. [github.com](https://github.com) → **New repository** → ارفع كل الملفات (`index.html`, `style.css`, `config.js`, `supabase-client.js`, `db.js`, `app.js`, `sw.js`, `schema.sql`, `SETUP.md`) بنفس المجلد الجذر.
2. [vercel.com](https://vercel.com) → سجّل دخول بـ GitHub → **Add New → Project** → اختر الريبو → **Deploy** (ما يحتاج أي إعداد إضافي، Vercel يتعرف عليه كموقع ثابت تلقائياً).
3. بعد نص دقيقة بيطلع لك رابط حقيقي شغال، وأي تحديث ترفعه على GitHub ينشر تلقائياً.

---

## أول تجربة

1. افتح الرابط → شاشة **تسجيل الدخول**.
2. "أنشئ حساب جديد" → اسمك، إيميلك، كلمة مرور → **إنشاء الحساب**.
3. لو "Confirm email" مفعّل، أكّد من بريدك ثم سجّل دخولك.
4. رصيدك يبدأ 50 🪙 تلقائياً، وحسابك حقيقي — تسجّل دخول من أي جهاز بنفس الإيميل وتلقى نفس بياناتك.

---

## ميزات ترقية v2

1. **من جاوب على سؤالك** يظهر باسمه وصورته ووقت إجابته تحت كل سؤال متجاوب عليه.
2. **زيارة الحسابات + رسائل خاصة**: اضغط أي اسم أو صورة (بالأسئلة، الاقتباسات، التعليقات) يفتحلك حسابه مع زر متابعة/مراسلة. تبويب "الرسائل" الجديد يجمع كل محادثاتك.
3. **الاعتراف باختيارك**: باسمك أو مجهول بالكامل — والصور الشخصية تظهر بكل مكان زي أي منصة اجتماعية.
4. **الشوت أوت** ينبّه كل الأعضاء تلقائياً فور نشره (عبر قاعدة البيانات مباشرة، مش JavaScript، فمافي طريقة يفوت أحد الإشعار).
5. **نظام عملات متوازن**: لايك على منشورك = 1 🪙، تجاوب على سؤال = 5 🪙، تسأل سؤال = 8 🪙.
6. **فئات الاقتباسات**: اختر فئة موجودة أو اكتب فئة جديدة عند النشر — تظهر تلقائياً للجميع كخيار فلترة.
7. **الاقتباس المميز**: أعلى اقتباس لايكات يترصّد ويثبّت لمدة 24 ساعة، ثم يُعاد الحساب تلقائياً (يتحدّث أول ما أي حد يفتح صفحة الاقتباسات، بدون حاجة لإعداد جدولة إضافية بـ Supabase).
8. **مشاركة لشخص محدد**: زر 📤 على أي منشور يفتح قائمة أشخاص تتابعهم/راسلتهم، تختار واحد وترسله له مباشرة كرسالة.
9. **الشعار QQC**: بخط كبير بارز، وتحته Question · Quote · Confession — بصفحة تسجيل الدخول.

---

## حدود واضحة (بصراحة)

- **الدفع الحقيقي**: ترقية VIP لسا محاكاة (تفعيل مباشر بدون بوابة دفع). ربط Stripe أو أي بوابة دفع حقيقية خطوة منفصلة لو تبيها.
- **صلاحيات التعديل**: أي مستخدم مسجل يقدر يجاوب على أي سؤال مفتوح (مقصود — تصميم "مجتمع يجاوب")، بس هذا يعني تقنياً إنه يقدر يعدّل صفوف غيره لو حاول يتلاعب بالكود مباشرة. لموقع إنتاجي حقيقي، يُنصح تضييق قواعد RLS أكثر لاحقاً.
- **صورة القصة 📤 (تصدير كصورة)**: تصدير حقيقي وتنزيل فعلي، بس تصميمها بسيط (نص على خلفية متدرجة) مو مطابق تماماً لتصميم البطاقة بالموقع. لا تلخّط بينه وبين زر "مشاركة لشخص" الجديد (٨ فوق) — الاثنين موجودين، كل واحد له غرضه.
- **الاقتباس المميز**: يتحدّث بس لما حد يفتح صفحة الاقتباسات (مش بجدولة تلقائية بالخلفية) — يعني لو ماحد فتح الصفحة لمدة طويلة، ممكن يتأخر التحديث لين أول زيارة بعد انتهاء الـ24 ساعة. كافي لأغلب الاستخدامات.
- **Service Worker محذوف نهائياً** — كان سبب مشكلة الكاش العالق سابقاً. الكود الحين يلغي أي نسخة قديمة منه تلقائياً من متصفح أي زائر.

أي شي من هذي تبي تطويره أكثر، قولي وأبدأ فيه.

